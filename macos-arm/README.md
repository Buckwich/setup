# Setup MacOS with Arm Processor

## Init

Should be run once to setup basic requriements:

* Brew
* Git
* GH CLI & Chrome to setup git auth

## Setup

Keeps the system on a consistent state:

* Installs Brewfile